spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/geobyte
    username: postgres
    password: 1234
  jpa:
    #properties:
      #hibernate:
        #dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true